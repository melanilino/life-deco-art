name: Match Aprende FAQ top gap

on:
  push:
    paths:
      - '.github/workflows/match-aprende-faq-top-gap.yml'

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Match FAQ top whitespace to bottom CTA whitespace
        run: |
          python3 <<'PY'
          from pathlib import Path
          p = Path('Aprende.dc.html')
          t = p.read_text(encoding='utf-8')
          old = "faqPad: m ? '48px 24px 0' : '72px 48px',"
          new = "faqPad: m ? '20px 24px 0' : '72px 48px',"
          if old not in t:
              raise SystemExit('faqPad mobile pattern not found')
          t = t.replace(old, new, 1)
          p.write_text(t, encoding='utf-8')
          if "faqPad: m ? '20px 24px 0' : '72px 48px'," not in t:
              raise SystemExit('faqPad guardrail failed')
          PY
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add 'Aprende.dc.html'
          git commit -m "Match Aprende FAQ top whitespace to lower spacing"
          git push
