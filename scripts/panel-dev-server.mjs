import http from 'node:http';import fs from 'node:fs/promises';import path from 'node:path';
const root=process.cwd(),port=Number(process.env.PANEL_DEV_PORT||4178),fixtures=process.argv.includes('--fixtures');
const types={'.html':'text/html','.mjs':'text/javascript','.js':'text/javascript','.css':'text/css','.svg':'image/svg+xml','.otf':'font/otf','.woff2':'font/woff2','.json':'application/json'};
http.createServer(async(req,res)=>{try{let route=decodeURIComponent(new URL(req.url,'http://localhost').pathname);if(route==='/panel'||route==='/')route='/panel.html';if(fixtures&&route==='/panel/store.mjs')route='/tests/panel-fixture-store.mjs';const file=path.resolve(root,'.'+route);if(!file.startsWith(root+path.sep)||route.includes('/.')){res.writeHead(403);res.end();return;}const data=await fs.readFile(file);res.writeHead(200,{'Content-Type':types[path.extname(file)]||'application/octet-stream','Cache-Control':'no-store'});res.end(data);}catch{res.writeHead(404);res.end('No encontrado');}}).listen(port,'127.0.0.1',()=>console.log(`Panel local http://127.0.0.1:${port}/panel · fixtures=${fixtures}`));
